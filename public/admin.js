// Placeholder admin.js
// This file prevents 404s while the proper bundled admin script is generated by the build.
// TODO: Replace with bundled admin client code (from src/scripts/admin) during build.

console.log('[admin.js] placeholder loaded');

// Simple helper to wait for Supabase readiness if other scripts expect it
window.addEventListener('supabaseReady', () => {
  console.log('[admin.js] supabaseReady event received');
  window.dispatchEvent(new CustomEvent('adminScriptLoaded'));
});
